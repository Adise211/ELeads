{
  "version": 2,
  "install": "npm install",
  "builds": [{ "src": "dist/server.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "/dist/server.js" }],
  "env": {
    "NODE_ENV": "production"
  }
}
