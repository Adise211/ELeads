{
  "name": "server",
  "version": "1.0.0",
  "description": "edgarLeads server",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/server.js",
    "dev": "npx tsc && node dist/server.js",
    "build": "npx tsc",
    "dev:watch": "npx tsc -w & nodemon dist/server.js"
  },
  "author": "Adise",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "nodemon": "^3.1.10",
    "prisma": "^6.10.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "dotenv": "^17.0.0",
    "express": "^5.1.0"
  }
}
