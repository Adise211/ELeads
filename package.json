{
  "name": "eleads",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:shared": "npm run build --workspaces=shared",
    "build:client": "npm run build --workspaces=client",
    "build:server": "npm run build --workspaces=server",
    "build": "npm run build:shared && npm run build:client && npm run build:server"
  },
  "author": "Adise",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.15",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "zod": "^4.0.10"
  }
}
