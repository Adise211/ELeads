{
  "name": "eleads",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:shared": "cd shared && npm run build",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "build": "npm run build:shared && npm run build:client && npm run build:server",
    "build:client-only": "npm run build:shared && npm run build:client",
    "update:shared": "cd shared && npm run build && cd ../server && npm install && cd ../client && npm install"
  },
  "author": "Adise",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^24.0.15",
    "@types/sanitize-html": "^2.16.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "sanitize-html": "^2.17.0",
    "zod": "^4.0.10"
  }
}
